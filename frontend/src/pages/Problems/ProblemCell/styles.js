import styled from 'styled-components';
import Cell from '~/components/Cell';
import Actions from '~/components/Actions';

export const ProblemItem = styled(Cell)``;

export const ProblemActions = styled(Actions)`
  margin-left: 60px;
`;
